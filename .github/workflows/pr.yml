name: CI

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize,edited,closed]
    paths:
      - 'access_request/accessRequest/**'      

jobs:
  build:
    runs-on: ubuntu-latest 
    name: Test changed-files
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  
          
      - name: Files changed after PR merge
        id: changes
        run: echo ::set-output name=changedFile::$(curl -s -X GET -G https://api.github.com/repos/${GITHUB_REPOSITORY}/pulls/${{ github.event.pull_request.number }}/files | jq -r '.[] | .filename')
      - name: List all added files
        run: |      
            echo ${{ steps.changes.outputs.changedFile }}       
      - name: startTime from Pull request merged_at
        id: startTime
        run: echo ::set-output name=timeStamp::$(jq -rRn --arg GITHUB_CONTEXT ${{github.event.pull_request.merged_at}} '$GITHUB_CONTEXT | fromdate')
      - name: echo timestamp
        run: |      
            echo ${{ steps.startTime.outputs.timeStamp }}   
      - name: validate access.json file        
        run: |
         cat ${{ steps.changes.outputs.changedFile }}|jq 
      - name: Read access.json
        id: readAccessFile
        uses: juliangruber/read-file-action@v1
        with:
            path: ${{ steps.changes.outputs.changedFile }}
            
      - name: Echo access.json   
        #run: echo "${{ steps.readAccessFile.outputs.content }}"   
        run: echo "${{fromJson(steps.readAccessFile.outputs.content).aws_username}}"
      - name: ConfigureAws  
        if: |
            github.event.pull_request.merged == true
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2
      - name: send message 
        if: |
            github.event.pull_request.merged == true
        uses: isbang/sqs-action@v0.1.1
        with:
          sqs-url: ${{ secrets.sqs_url }}
          message: "userName: ${{fromJson(steps.readAccessFile.outputs.content).aws_username}}, starttime: ${{ steps.startTime.outputs.timeStamp }}, Duration: ${{fromJson(steps.readAccessFile.outputs.content).timeDuration_in_seconds}}"
